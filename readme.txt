I am in progress with the SonataODS data exploration. Completed the initial churn/retention understanding, table-column review, relationship check, and blank/unusable column identification. Currently working on data quality checks. No blockers.


Basic data quality checks means I am checking whether the important columns are usable for analysis/modeling. For example, I am checking row count, null count, distinct values, duplicate keys, date ranges, and whether key columns can join properly across tables.
